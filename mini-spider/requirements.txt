# === Dependencias base (Fase 1) ===
# FastAPI: framework web para crear la API y servir el frontend
fastapi==0.115.0
# Uvicorn: servidor que ejecuta la aplicación FastAPI (con extras como recarga automática)
uvicorn[standard]==0.30.6
# Jinja2: motor de plantillas HTML (lo usamos para el frontend embebido en el backend)
jinja2==3.1.4
# httpx: cliente HTTP asíncrono, lo usaremos para consultar APIs externas (crt.sh, ip-api, etc.)
httpx==0.27.2
# python-multipart: necesario para que FastAPI reciba datos de formularios HTML
python-multipart==0.0.9

# === Fase 3 ===
# dnspython: librería para hacer consultas DNS (A, AAAA, MX, TXT, NS, CNAME)
dnspython==2.6.1

# === Fase 4 ===
# python-whois: consulta el protocolo WHOIS (puerto 43) sin depender de
# tener el binario "whois" instalado en el sistema
python-whois==0.9.6

# === Fase 7 ===
# cryptography: parsea certificados X.509 (SSL/TLS) sin validar la cadena
# de confianza, para poder inspeccionar certificados autofirmados o vencidos
cryptography==50.0.1
